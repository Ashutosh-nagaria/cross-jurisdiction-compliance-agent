# Copy this file to .env and fill in real values there.
# .env is in .gitignore and will never be committed. This file only shows
# which keys are needed, with no real values in it.

ANTHROPIC_API_KEY=
VOYAGE_API_KEY=
MONGODB_ATLAS_URI=
LANGSMITH_API_KEY=
